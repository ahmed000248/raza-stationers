Run Phase 0 (repo/safety assessment) read-only, exactly as your workflow describes — but this is not a greenfield repo, so treat the following as ground truth before you analyze anything, not as something to rediscover:

**Real current structure (do not rename):** `apps/web` (customer site, already built and QA-passed — do not touch or rename to "storefront"), `apps/admin` (admin panel, mid-build), `apps/mobile` (empty placeholder, out of scope until backend is done). `packages/types`, `packages/validation`, `packages/ui`, `packages/db`, `packages/api` — keep these names, do not introduce `shared-types`/`shared-validation`.

**Naming collision to be aware of:** `packages/api` already exists but is a placeholder types/stub package — it is NOT the NestJS server. The new NestJS backend you're building should live at `apps/api`. Tell me explicitly how you'll avoid confusing the two before writing anything there.

**A schema already exists — this is your Phase 0/1 baseline, not a blank slate:** `packages/db/prisma/schema.prisma` already implements all 20 entities from `docs/TRD.md` §6 / `packages/types/src/index.ts`, including a resolved pricing model: `Product.wholesalePrice` (required) + `retailPrice`/`buyingPrice` (optional, pending), with the 5-tier price-resolution order documented in `docs/BRD.md` PR-01/CD-01 and `docs/FRD.md` §8. Read these three files plus `docs/website/architecture.md` and `docs/admin/architecture.md` before proposing any schema changes. If you think something there is wrong, say so explicitly — don't silently redesign it.

**Scope boundary — flag, don't default into:** your usual conceptual model for this kind of system includes a full price-list/tier engine (`PriceList`, `ClientPriceTier`, `ClientProductPrice`, `PriceHistory`), multi-warehouse support, `Supplier`/`PurchaseReceipt`, `Return`/`ReturnItem`, `Role`/`Permission` tables, and `ProductImage`. None of these are in the locked BRD/FRD/TRD scope for v1 — the full discount rules engine is explicitly BRD CD-02 "a later phase," and `ProductImage` directly contradicts the finalized no-product-photography catalogue design. If your analysis says any of these are actually needed now, list them as open questions for me and the business owner to approve — do not add them by default.

**One addition that's welcome:** an `ImportBatch`/`ImportRow`/`ImportError` staging model for the catalogue importer (Phases 9–10) isn't in the current schema and is a good, low-risk gap to propose filling.

**Source data:** the real catalogue is `RS-Database.xlsx` — 2,156 products, 87 categories, wholesale prices only (retail/buying prices arrive separately). 136 rows need review (63 uncertain category, 58 duplicate-name-different-price, 13 zero-price, 2 exact duplicates) — treat these as known, already-profiled, not something to re-discover from scratch.

Deliver a Phase 0 report: confirm/correct the directory plan against what's actually here, confirm where `apps/api` should live and how it relates to `packages/api`, and list every open scope question above that needs an explicit yes/no from us before Phase 3 conceptual modeling starts. Do not write or edit any files yet.
