export default function AdminPlaceholderPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="max-w-md text-center">
        <h1 className="font-heading text-2xl font-semibold">Raza Stationers Admin</h1>
        <p className="mt-2 text-sm text-muted-foreground">
          Scaffold only — no admin pages have been built yet. See
          docs/admin/phases.md for the approved build order; each phase needs
          explicit sign-off before its code is written.
        </p>
      </div>
    </main>
  );
}
